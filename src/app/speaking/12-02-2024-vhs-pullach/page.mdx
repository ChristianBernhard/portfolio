import {SpeakingLayout} from '@/components/SpeakingLayout';
import {Button} from '@/components/Button'

export const speakingEvent = {
    speaker: 'Christian Bernhard',
    language: "🇩🇪",
    date: '2024-12-02',
    location: 'vhs Pullach',
    title: 'Von KI-Boom zu Chip-Hype: Warum Nvidia & Co. die Zukunft der Technologie gestalten',
    description: 'Entdecken Sie die faszinierende Welt der Künstlichen Intelligenz und der Hardware, die sie möglich macht. In diesem Kurs beleuchten wir, warum der rasante Fortschritt im Bereich der KI die Nachfrage nach spezialisierter Hardware, wie GPUs und NPUs, explodieren lässt. Sie erfahren, wie Rechenleistung in der modernen KI-Ära gemessen wird, warum GPUs gegenüber herkömmlichen CPUs überlegen sind und welche Rolle Tech-Giganten wie Nvidia und TSMC in dieser Revolution spielen.',
};

export const metadata = {
    title: speakingEvent.title,
    description: speakingEvent.description,
};

export default function SpeakingEventDetail(props) {
    return (
        <SpeakingLayout event={speakingEvent} {...props}>
            <h2 className="section-heading">Beschreibung</h2>
            <p className="course-description">Entdecken Sie die faszinierende Welt der Künstlichen Intelligenz und der
                Hardware, die sie möglich macht. In diesem Kurs beleuchten wir, warum der rasante Fortschritt im Bereich
                der KI die Nachfrage nach spezialisierter Hardware, wie GPUs und NPUs, explodieren lässt. Sie erfahren,
                wie Rechenleistung in der modernen KI-Ära gemessen wird, warum GPUs gegenüber herkömmlichen CPUs
                überlegen sind und welche Rolle Tech-Giganten wie Nvidia und TSMC in dieser Revolution spielen.</p>
            <div className="flex justify-center">
                <Button
                    href="https://www.vhs-pullach.de/suche/kursdetails.html?courseId=503-C-5030742"
                    variant="primary" className="mt-4">
                    Melden Sie sich jetzt an!
                </Button>
            </div>
        </SpeakingLayout>
    );
}
